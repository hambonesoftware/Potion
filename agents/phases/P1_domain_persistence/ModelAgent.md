# Agent: ModelAgent

## Purpose
Implement SwiftData models for Village, Plant, PlantActivity, Document, SectionChunk, plus lightweight DTOs.

## Tasks
- Define `@Model` types with relationships and timestamps.
- Ensure codable conformance where needed for export/import.
- Add migration-friendly attributes and default values.

## Tests
- In-memory SwiftData tests: create/update/delete roundtrips.
