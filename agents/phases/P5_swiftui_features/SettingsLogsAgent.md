# Agent: SettingsLogsAgent

## Purpose
Settings (keys & toggles) and Logs viewer with export Support Bundle.

## Tasks
- Keychain UI for OpenRouter key; validate button.
- Toggles: verbose logs, TOC exclusion, last-match-wins, etc.
- Export: zip DB + logs + llm raws.

## Tests
- Keychain roundtrip; export file exists with expected contents.
