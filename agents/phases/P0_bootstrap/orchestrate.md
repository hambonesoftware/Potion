# P0 Orchestration

## Goal
Create SwiftUI app skeleton with modules, config/logging, and an optional wrapper around `plantit_html/`.

## Agents
- iOSProjectAgent
- ConfigLoggingAgent
- LegacyWrapperAgent (optional, parity mode)

## Exit
- App launches on iPad; wrapper loads `plantit_html` when enabled.
