# agents/ — Phase-Aligned Agent Prompts

This folder contains the orchestration and agent prompts for each phase of the Plantit native SwiftUI 6+ port. 
Use the `orchestrator.md` as the entry point. For each phase, run the `orchestrate.md` then execute agent files in order.
