# UI Map

- **Home Tabs**
  - Villages → Plants Grid → Plant Detail → Activity Timeline → Add/Edit
  - Documents → List → Import PDF → Parse → Headers → Sections → Diff ("Golden vs Matched")
  - Search → Results (Sections, Lines) → Jump-to
  - Settings → API Key, Verbose logs, Developer tools (dump DB, export bundle), Feature toggles
- **Logs Viewer**
  - Per document logs and LLM raw JSON viewer.
