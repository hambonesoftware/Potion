# Logging & Tracing

- OSLog categories: `parse`, `headers`, `match`, `search`, `ui`, `net`
- For each long task: start/end stamps, counts (pages/lines/headers/sections), durations.
- LLM: token estimates, model name, latency, retries, raw response path.
- Export bundle: zipped DB, logs, configs, document meta.
