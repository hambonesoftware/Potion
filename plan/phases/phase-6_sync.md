# Phase 6 — Sync/Interop (Optional)

**Goal**: Optional sync with remote or desktop tools; maintain compatibility with Python server if desired.

## Tasks
- Export domain records to JSON bundles for backup/sharing.
- Optional: REST sync client with conflict strategy (last-write-wins) and audit log.
- Background tasks for index maintenance and VACUUM.

## Acceptance
- Enable/disable sync; observe predictable, logged behavior.
